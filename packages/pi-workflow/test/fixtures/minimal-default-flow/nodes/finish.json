{
  "id": "finish",
  "executor": { "type": "return" },
  "dependsOn": ["collect"],
  "inputs": {
    "result": { "from": "context", "path": "collected" }
  },
  "output": { "to": "final", "mergeStrategy": "replace" }
}
